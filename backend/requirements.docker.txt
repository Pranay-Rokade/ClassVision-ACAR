# ============================================================
# ClassVision-ACAR — Docker-specific requirements
# 
# Only lists TOP-LEVEL packages the code actually imports.
# pip will auto-resolve all sub-dependencies for the correct
# platform (Linux ARM64, x86_64, etc.)
#
# DO NOT pin platform-specific packages here (nvidia-*, triton, etc.)
# ============================================================

# --- Django & REST Framework ---
Django==5.1.5
djangorestframework==3.15.2
django-cors-headers==4.7.0

# --- ML / Computer Vision ---
tensorflow==2.19.0
mediapipe==0.10.18
ultralytics>=8.3.0
opencv-contrib-python==4.11.0.86
face-recognition==1.3.0
numpy==1.26.4

# --- Data Processing ---
pandas==2.2.3

# --- Utilities ---
python-dotenv==1.1.0
psutil==7.0.0
requests==2.32.3
